<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>com.amx.jax</groupId>
	<artifactId>jax</artifactId>
	<version>${jax.version}</version>
	<packaging>pom</packaging>

	<name>jax-system</name>
	<url>http://maven.apache.org</url>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

		<jax.version>0.0.1-SNAPSHOT</jax.version>
		<jax.lib.amx.version>0.0.1-SNAPSHOT</jax.lib.amx.version>
		<jax.core.version>0.0.1-SNAPSHOT</jax.core.version>
		<jax.ms.user.version>0.0.1-SNAPSHOT</jax.ms.user.version>
		<jax.ms.meta.version>0.0.1-SNAPSHOT</jax.ms.meta.version>
		<jax.client.version>0.0.1-SNAPSHOT</jax.client.version>
	</properties>

	<dependencies>
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>
	<build>
		<plugins>
			<plugin>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<source>1.8</source>
					<target>1.8</target>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>1.5.8.RELEASE</version>
	</parent>

	<modules>
		<module>boot-utils</module>
		<module>amx-lib</module>
		<module>core</module>
		<module>ms-meta</module>
		<module>ms-user</module>
		<module>ms-jax</module>
		<module>jax-client</module>
		<module>server-ui</module>
	</modules>
</project>
